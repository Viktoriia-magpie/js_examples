function isFruit(name){
    if (name == "apple") {
        console.log(name + " is a fruit.");
    }
    else if (name == "banana") {
        console.log(name + " is a fruit.");        
    } 
    else {
        console.log("I have no idea what is "+ name);
    }
}

isFruit("radio")